<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xzone">
    <!-- Permissions for Bluetooth access -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".ForegroundRun"
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Xzone">
        <activity android:name=".DeleteActivity"></activity>
        <activity android:name=".SettingsActivity" />
        <activity android:name=".RestartOldZoneActivity" />
        <activity android:name=".TermsActivity" />
        <activity android:name=".StatActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".SetZoneActivity" />
        <activity android:name=".SelectDeviceActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".ServiceClass" />
    </application>

</manifest>